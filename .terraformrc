provider_installation {
  # Prefer Terraform Registry as a network mirror
  network_mirror {
    url    = "https://registry.terraform.io/"
    include = ["registry.terraform.io/*/*"]
  }

  # Fallback to direct (in case a provider isn’t mirrored)
  direct {
  }
}
