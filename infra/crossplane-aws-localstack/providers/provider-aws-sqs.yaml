apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-aws-sqs
  labels:
    argocd.argoproj.io/instance: crossplane-providers
spec:
  # Match your family version if you want, e.g. v2.2.0; or keep v0.40.0 like S3.
  package: xpkg.upbound.io/upbound/provider-aws-sqs:v2.2.0
  controllerConfigRef:
    name: aws-localstack-cc
  runtimeConfigRef:
    apiVersion: pkg.crossplane.io/v1beta1
    kind: DeploymentRuntimeConfig
    name: default
