apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-aws-s3
  namespace: crossplane-system
spec:
  # keep it aligned with provider-aws; family split uses the same controllerConfig
  package: xpkg.upbound.io/upbound/provider-aws-s3:v1.15.0
  controllerConfigRef:
    name: localstack-cc
